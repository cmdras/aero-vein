# Generated files — excluded from formatting (oxfmt/vp respects .prettierignore).
# These are regenerated by their tools and must not be hand- or auto-formatted:
#   routeTree.gen.ts — TanStack Router plugin (regenerated every build)
#   src/api/schema.ts — openapi-typescript (gen:api)
# Mirrors the same exclusions already in .oxlintrc.json and .fallowrc.json.
src/routeTree.gen.ts
src/api/schema.ts
dist
